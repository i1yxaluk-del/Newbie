# --- App ---
APP_NAME=Domik Alina
APP_ENV=dev
APP_SECRET=change-me-please-very-long-random-string
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# --- Admin (создаётся при первом запуске, если БД пустая) ---
ADMIN_EMAIL=admin@domik.local
ADMIN_PASSWORD=ChangeMe2026!

# --- SMTP (привяжем позже личный ящик) ---
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=465
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_USE_TLS=true
NOTIFY_EMAIL_TO=

# --- Telegram (бот создадим позже) ---
TG_BOT_TOKEN=
TG_CHAT_ID=

# --- DB ---
DATABASE_URL=sqlite:///./data/domik.db
